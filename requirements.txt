# JeiGuard AI v2.0.0 — Dependencias
# Copyright © 2026 Jeiner Tello Nuñez — MIT License
#
# Instalación mínima para demo:
#   pip install numpy scikit-learn
#
# Instalación completa:
#   pip install -r requirements.txt

# ── Core ML ───────────────────────────────────────────────────────────────────
numpy>=1.26.0
scikit-learn>=1.3.0
scipy>=1.11.0
joblib>=1.3.0

# ── Interfaz web ──────────────────────────────────────────────────────────────
streamlit>=1.29.0
plotly>=5.18.0
pandas>=2.1.0

# ── API REST ──────────────────────────────────────────────────────────────────
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic[email]>=2.5.0
httpx>=0.25.0

# ── Infraestructura de mensajería ─────────────────────────────────────────────
confluent-kafka>=2.3.0
elasticsearch>=8.11.0
websockets>=12.0

# ── Base de datos (v2.0.0) ────────────────────────────────────────────────────
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0          # Driver async para PostgreSQL
alembic>=1.13.0          # Migraciones de base de datos
redis[hiredis]>=5.0.0    # Cache y sesiones

# ── Autenticación y seguridad (v2.0.0) ───────────────────────────────────────
bcrypt>=4.1.0            # Hash de contraseñas
python-jose[cryptography]>=3.3.0  # JWT tokens
passlib[bcrypt]>=1.7.4   # Utilidades de contraseñas

# ── Reportes PDF (v2.0.0) ────────────────────────────────────────────────────
reportlab>=4.1.0         # Generación de PDFs profesionales

# ── CVE / Threat Intelligence ─────────────────────────────────────────────────
# (reutiliza httpx para consultas a NVD API)

# ── MLflow (v2.0.0) ───────────────────────────────────────────────────────────
mlflow>=2.9.0            # Model registry y experiment tracking
# mlflow[sklearn]        # Opcional: auto-logging para scikit-learn
# mlflow[tensorflow]     # Opcional: auto-logging para TensorFlow

# ── OpenTelemetry (v2.0.0) ───────────────────────────────────────────────────
opentelemetry-api>=1.22.0
opentelemetry-sdk>=1.22.0
opentelemetry-exporter-otlp-proto-grpc>=1.22.0
opentelemetry-exporter-jaeger>=1.21.0
opentelemetry-instrumentation-fastapi>=0.43b0
opentelemetry-instrumentation-httpx>=0.43b0
opentelemetry-instrumentation-sqlalchemy>=0.43b0

# ── LLM Analyst ───────────────────────────────────────────────────────────────
anthropic>=0.20.0

# ── Kubernetes Operator (v2.0.0) ──────────────────────────────────────────────
# kopf>=1.36.0           # Kubernetes operator framework
# kubernetes-asyncio>=24.2.0  # Kubernetes async client

# ── SDK Python (v2.0.0) ───────────────────────────────────────────────────────
# (reutiliza httpx y websockets)

# ── Opcionales ────────────────────────────────────────────────────────────────
# tensorflow>=2.14.0    # CNN-1D + Autoencoder
# shap>=0.44.0          # XAI con SHAP real
# imbalanced-learn>=0.11.0  # Balanceo de clases

# ── Calidad de código ─────────────────────────────────────────────────────────
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-asyncio>=0.23.0   # Tests async
mypy>=1.7.0
ruff>=0.1.0
bandit>=1.7.0            # Security linting
